#!/usr/bin/env bash
#
# update local installs

brew update && brew upgrade && brew cleanup && brew prune
brew upgrade brew-cask && brew cask cleanup
brew outdated

gem update --system && gem update && gem clean
rvm get stable
rvm cleanup all
rvmGemsetsCleanup

sudo npm update -g

# Custom cleaners
rebuildFinderOpenWith
